using System;
using System.Collections.Generic;

/* 
** 给定一个字符串 s，找到 s 中最长的回文子串。你可以假设 s 的最大长度为 1000。
*/

namespace LeetCode
{
    public class Solution
    {
        public string LongestPalindrome(string s)
        {
            for (int i = 0; i < s.Length; i++)
            {
                int leftlength = i - 0;
                int rightLength = s.Length - i;

            }

            return null;
        }
    }
}